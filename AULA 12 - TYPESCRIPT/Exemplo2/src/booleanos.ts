// boolean (Primitivo) - Usar preferencialmente
let verdade: boolean = true;

// Boolean (Objeto)
let verdadeiro: Boolean = true;

let tarefaConcluida: boolean = true;
let tarefaPendente: boolean = false;

console.log(tarefaConcluida);
console.log(tarefaPendente);

let concluido: boolean = true;

if(concluido){
    console.log('concluida com sucesso');
} else {
    console.log('tarefa pendente');
}